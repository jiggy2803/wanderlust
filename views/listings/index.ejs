<% layout('/layouts/boilerplate') %>
  <style>
    #filters {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
    }

    .filter {
      text-align: center;
      margin-right: 1.5rem;
      margin-top: 1rem;
      opacity: 0.5;
      text-decoration: none;
      color: black;
      
    }
    .filter a{
      text-decoration: none;
      color: black;

    }

    .filter:hover {
      opacity: 1;
      cursor: pointer;
    }

    .filter p {
      font-size: 0.8rem;
    }

    .tax-info {
      display: none;
    }

    .tax-toggle {
      border: 1px solid black;
      border-radius: 1rem;
      height: 2.5rem;
      padding: 1rem;
      margin-left: 0.4rem;
      display: flex;
      align-items: center;
    }
  </style>
  <div id="filters">
    <!-- <div class="filter">
      <% const categories=["Trending",  ,  ,  ,  , 
        ,  ,  ,  , "" , "" , "" ]; %>

        <% categories.forEach(cat=> { %>
          <a href="/listings?<%= `category=${cat}` %>" class="filter <%= selectedCategory === cat ? 'active' : '' %>">
            <%= cat %>
          </a>
          <% }) %>
    </div> -->

    <div class="filter">
      <a  href="/listings?<%= `category=${"Trending"}` %>"  >
        <i class="fa-solid fa-fire"></i>
        <p>Trending</p>
      </a>
    </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Rooms"}` %>">
        <i class="fa-solid fa-bed"></i>
        <p>Rooms</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Iconic cities"}` %>">
        <i class="fa-solid fa-mountain-city"></i>
        <p>Iconic cities</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Mountains"}` %>">
        <i class="fa-solid fa-mountain-sun"></i>
        <p>Mountains</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Top Buildings"}` %>">
        <i class="fa-solid fa-city"></i>
        <p>Top Buildings</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Amazing Pools"}` %>">
        <i class="fa-solid fa-person-swimming"></i>
        <p>Amazing Pools</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Camping"}` %>">
        <i class="fa-solid fa-campground"></i>
        <p>Camping</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Beach Resorts"}` %>">
        <i class="fa-solid fa-umbrella-beach"></i>
        <p>Beach Resorts</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Arctic"}` %>">
        <i class="fa-solid fa-snowflake"></i>
        <p>Arctic</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Farms"}` %>">
        <i class="fa-solid fa-cow"></i>
        <p>Farms</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Top cities"}` %>">
        <i class="fa-solid fa-tree-city"></i>
        <p>Top cities</p>
      </a>
  </div>
  <div class="filter">
    <a href="/listings?<%= `category=${"Most visited"}` %>">
        <i class="fa-solid fa-users"></i>
        <p>Most visited</p>
      </a>
  </div>

  <div class="tax-toggle">
    <div class="form-check-reverse form-switch">
      <input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
      <label class="form-check-label" for="switchCheckDefault">Display Total after taxes</label>
    </div>
  </div>
  </div>
  <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
    <%for (let listing of allListings){%>
      <div class="card col listing-card">
        <img src="<%=listing.image.url%>" class="card-img-top" style="height: 20rem;" alt="Listing image">
        <div class="card-body">
          <h5 class="card-title">
            <%=listing.title%>
          </h5>
          <p class="card-text">&#8377; <%=listing.price.toLocaleString("en-IN")%>/ Night <i
                class="tax-info">&nbsp;&nbsp; + 18% GST</i></p>
          <a href="/listings/<%= listing._id%>" class="btn btn-primary">Book Place</a>
        </div>
      </div>

      <%}%>
  </div>

  <script>
    let taxSwitch = document.getElementById("switchCheckDefault")
    taxSwitch.addEventListener("click", () => {
      let taxInfo = document.getElementsByClassName("tax-info");
      for (info of taxInfo) {
        if (info.style.display != "inline") {
          info.style.display = "inline"
        } else {
          info.style.display = "none"
        }
      }
    })
  </script>